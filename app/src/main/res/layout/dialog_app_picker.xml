<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_card_3d_glass"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvPickerTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="PILIH APLIKASI PINTASAN"
            android:textColor="@color/accent_amber"
            android:textSize="14sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnPickerClose"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:text="TUTUP [X]"
            android:textColor="@color/text_secondary"
            android:textSize="11sp" />
    </LinearLayout>

    <!-- Search Input -->
    <EditText
        android:id="@+id/etPickerSearch"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="10dp"
        android:background="@drawable/bg_button_shortcut"
        android:hint="Ketik untuk mencari aplikasi..."
        android:paddingHorizontal="12dp"
        android:textColor="@color/text_primary"
        android:textColorHint="@color/text_dim"
        android:textSize="12sp" />

    <!-- List of Apps -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvPickerApps"
        android:layout_width="match_parent"
        android:layout_height="280dp"
        android:scrollbars="vertical" />

</LinearLayout>
